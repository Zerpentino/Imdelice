<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Imdeliceapp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:pages="clr-namespace:Imdeliceapp.Pages"
Shell.FlyoutBehavior="Disabled"
    Title="Imdeliceapp"
   Shell.NavBarIsVisible="False"
   
    >

    
 
    <TabBar>
   <ShellContent Title="Tomar pedidos" Icon="contratos_on.png"
                ContentTemplate="{DataTemplate pages:TakeOrderPage}"
                Route="TakeOrderPage" />

    <ShellContent
      Title="Órdenes"
      Icon="tab_ordenes.png"
      ContentTemplate="{DataTemplate pages:OrdersPage}"
      Route="OrdersPage" />

    <ShellContent
      Title="Opciones"
      Icon="perfil_off.png" 
      ContentTemplate="{DataTemplate pages:OptionsPage}"
      Route="OptionsPage" />



</TabBar>

</Shell>
